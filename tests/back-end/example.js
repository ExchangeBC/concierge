describe("Example", () => {
  describe("action", () => {
    context("when condition", () => {
      it("outcome", () => {
        assert(true, "The example is working!");
      });
    });
  });
});
